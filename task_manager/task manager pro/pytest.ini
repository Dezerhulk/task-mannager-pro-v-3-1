[pytest]
# Pytest configuration for Task Manager Pro

python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests

addopts = 
    -v
    --strict-markers
    --tb=short

testpaths = tests
minversion = 7.0
